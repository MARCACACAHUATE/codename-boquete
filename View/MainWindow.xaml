<Window x:Class="codename_boquete.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:codename_boquete"
        mc:Ignorable="d"
        Title="Formulario Mamamlon" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label>Aqui ira el pinche formulario</Label>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Username"/>
            <TextBox />
            <TextBlock Text="Password"/>
            <TextBox />
            <TextBlock Text="email"/>
            <TextBox />
            <TextBlock Text="Tu pinche cola"/>
            <TextBox />

        </StackPanel>
        
        
    </Grid>

</Window>
